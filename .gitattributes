# MDX mishandles CRLF source files on Windows (doubling the CR), so keep all
# markdown (incl. README) and the shared doc/prelude.ml checked out as LF.
*.md text eol=lf
doc/prelude.ml text eol=lf
CHANGES.md whitespace=-blank-at-eol
